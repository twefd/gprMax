# Streamlit config for GPR Concrete Studio.
[browser]
# Don't send usage statistics.
gatherUsageStats = false

[server]
# Auto-reload the app when source files change (handy during development).
runOnSave = true
